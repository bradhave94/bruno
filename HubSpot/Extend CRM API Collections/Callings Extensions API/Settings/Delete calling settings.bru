meta {
  name: Delete calling settings
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/crm/v3/extensions/calling/:appId/settings
  body: none
  auth: bearer
}

auth:bearer {
  token: {{LL_PA}}
}

headers {
  Accept: */*
}
