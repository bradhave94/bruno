meta {
  name: Delete settings
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/crm/v3/extensions/videoconferencing/settings/:appId
  body: none
  auth: bearer
}

auth:bearer {
  token: {{LL_PA}}
}

headers {
  Accept: */*
}
