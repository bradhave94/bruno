meta {
  name: Cancel an active import
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/crm/v3/imports/:importId/cancel
  body: none
  auth: bearer
}

auth:bearer {
  token: {{LL_PA}}
}

headers {
  Accept: application/json
}
