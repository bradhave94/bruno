meta {
  name: Update
  type: http
  seq: 3
}

patch {
  url: {{baseUrl}}/crm/v3/objects/goal_targets/:goalTargetId?idProperty=<string>
  body: json
  auth: bearer
}

query {
  idProperty: <string>
}

auth:bearer {
  token: {{LL_PA}}
}

headers {
  Content-Type: application/json
  Accept: application/json
}

body:json {
  {
    "properties": {
      "Excepteur_c": "<string>",
      "occaecat6": "<string>",
      "esseb8": "<string>"
    }
  }
}
