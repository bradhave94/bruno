meta {
  name: Archive
  type: http
  seq: 2
}

delete {
  url: {{baseUrl}}/crm/v3/objects/tickets/:ticketId
  body: none
  auth: bearer
}

auth:bearer {
  token: {{LL_PA}}
}

headers {
  Accept: */*
}
