meta {
  name: Update
  type: http
  seq: 3
}

patch {
  url: {{baseUrl}}/crm/v3/objects/discounts/:discountId?idProperty=<string>
  body: json
  auth: bearer
}

query {
  ~idProperty: <string>
}

auth:bearer {
  token: {{LL_PA}}
}

headers {
  Content-Type: application/json
  Accept: application/json
}

body:json {
  {
    "properties": {
      "quis03f": "<string>"
    }
  }
}
