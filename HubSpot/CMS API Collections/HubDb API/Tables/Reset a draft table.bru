meta {
  name: Reset a draft table
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/cms/v3/hubdb/tables/:tableIdOrName/draft/reset?includeForeignIds=<boolean>
  body: none
  auth: none
}

query {
  includeForeignIds: <boolean>
}

headers {
  Accept: application/json
}
