meta {
  name: Get a row from the draft table
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/cms/v3/hubdb/tables/:tableIdOrName/rows/:rowId/draft
  body: none
  auth: bearer
}

auth:bearer {
  token: {{LL_PA}}
}

headers {
  Accept: application/json
}
