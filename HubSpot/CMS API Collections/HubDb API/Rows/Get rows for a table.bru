meta {
  name: Get rows for a table
  type: http
  seq: 8
}

get {
  url: {{baseUrl}}/cms/v3/hubdb/tables/:tableIdOrName/rows?sort=<string>&sort=<string>&after=<string>&limit=<integer>&properties=<string>&properties=<string>
  body: none
  auth: none
}

query {
  sort: <string>
  sort: <string>
  after: <string>
  limit: <integer>
  properties: <string>
  properties: <string>
}

headers {
  Accept: application/json
}
