meta {
  name: delete--webhooks-v3--app Id--settings clear
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/webhooks/v3/:appId/settings
  body: none
  auth: bearer
}

auth:bearer {
  token: {{LL_PA}}
}

headers {
  Accept: */*
}
