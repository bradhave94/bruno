meta {
  name: Get revisions of a marketing email
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/marketing/v3/emails/:emailId/revisions?after=<string>&before=<string>&limit=<integer>
  body: none
  auth: bearer
}

query {
  after: <string>
  before: <string>
  limit: <integer>
}

auth:bearer {
  token: {{LL_PA}}
}

headers {
  Accept: application/json
}
