meta {
  name: Restore a revision of a marketing email
  type: http
  seq: 11
}

post {
  url: {{baseUrl}}/marketing/v3/emails/:emailId/revisions/:revisionId/restore
  body: none
  auth: bearer
}

auth:bearer {
  token: {{LL_PA}}
}

headers {
  Accept: */*
}
