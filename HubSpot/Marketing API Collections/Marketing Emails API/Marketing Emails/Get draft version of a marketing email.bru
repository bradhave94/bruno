meta {
  name: Get draft version of a marketing email
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/marketing/v3/emails/:emailId/draft
  body: none
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{LL_PA}}
}
