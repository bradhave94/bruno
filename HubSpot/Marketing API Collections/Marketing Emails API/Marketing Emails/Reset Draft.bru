meta {
  name: Reset Draft
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/marketing/v3/emails/:emailId/draft/reset
  body: none
  auth: bearer
}

auth:bearer {
  token: {{LL_PA}}
}

headers {
  Accept: */*
}
